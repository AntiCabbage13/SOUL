/* import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const healthProf= () => {
  const styles = StyleSheet.create({
    container: {
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
    },
    text: {
      textAlign: 'center',
      fontSize: 24,
      fontWeight: 'bold',
    },
  });

  return (
    <View style={styles.container}>
      <Text style={styles.text}>!!!!!!!!</Text>
      <Text style={styles.text}>Somebody call an ambulance!!</Text>
    </View>
  );
};

export default healthProf;
 */